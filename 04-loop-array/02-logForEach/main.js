const array = ['Mike', 'Sid', 'Jack', 'Bill'];

function logPerson(person, order) {
    console.log(order + 1, person )
}

// name.forEach ( (person, order) => console.log(order + 1 , person)); 
// names.forEach(logPerson);
names.forEach(logPerson); 
